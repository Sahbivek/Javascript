"use strict" // treat all JS code as a newer version
// alert("hello boss")      cant use right now but later can be done
console.log(3+3);

let name="bivek" // String
let age = 21;   // number 2 to the power 53
let weight = 70.23  
let isFat = false  // boolean true/false
let relationship= null // null=> standalone value
// undefined leaves no value assigned
// symbol =>unique
// object 
console.log(typeof isFat);/// returns the type of data
console.log(typeof relationship);